<% @articles.each do |article| %>
  <h3>
    <marquee>
      <%= link_to article.title, article %>
    </marquee>
  </h3>
  <p>
    <%= article.content %>
  </p>
<% end %>
<%= link_to 'Create New Article', new_article_path %>
