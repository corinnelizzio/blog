<% @articles.each do |article| %>
  <h3>
    <%= link_to article.title, article %>
  </h3>
  <h5>
    <%= article.date %>
  </h5>
  <p>
    <%= article.content if user_signed_in? %>
  </p>
<% end %>
<%= link_to 'Create New Article', new_article_path if user_signed_in? %>
