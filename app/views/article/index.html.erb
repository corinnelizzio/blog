<% @articles.each do |article| %>
  <h3>
    <marquee>
      <blink>
        <%= link_to article.title, article %>
      </blink>
    </marquee>
  </h3>
  <p>
    <%= article.content %>
  </p>
<% end %>
<%= link_to 'Create New Article', new_article_path %>
